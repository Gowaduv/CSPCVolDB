<%= form_for(@staff) do |f| %>
  <% if @staff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@staff.errors.count, "error") %> prohibited this staff from being saved:</h2>

      <ul>
      <% @staff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event %><br>
    <%= f.collection_select(:event, Event.order(:name), :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :position %><br>
    <%= f.collection_select(:position, Position.order(:name), :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :shift %><br>
    <%= f.collection_select(:shift, Shift.order(:start, :duration), :id, :start_with_duration) %>
  </div>
  <div class="field">
    <%= f.label :permanent_user %><br>
    <%= f.collection_select(:user, Users.qualified(), :id, :username) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
