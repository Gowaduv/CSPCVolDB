<!-- uses app/assets/javascripts/approve_offer.js.coffee.erb -->
<% o = s.offers.pending.first if o.nil?%>
<%= link_to('Approve offer', "javascript:void(0);", { id: "approve_offer_"+o.id.to_s, class: "approve_offer", data:{ user_id: o.user.id} } ) %>
