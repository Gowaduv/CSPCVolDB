<!-- uses app/assets/javascripts/deny_offer.js.coffee.erb -->
<% o = s.offers.pending.first if o.nil?%>
<%= link_to('Deny offer', "javascript:void(0);", { id: "deny_offer_"+o.id.to_s, class: "deny_offer", data:{ user_id: o.user.id} } ) %>
